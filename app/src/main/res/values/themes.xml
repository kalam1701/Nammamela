<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NammaMela" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary = Forest Green -->
        <item name="colorPrimary">@color/forest_green</item>
        <item name="colorPrimaryVariant">@color/forest_green_dark</item>
        <item name="colorOnPrimary">@color/text_on_dark</item>

        <!-- Secondary = Gold Amber -->
        <item name="colorSecondary">@color/gold</item>
        <item name="colorSecondaryVariant">@color/gold_dark</item>
        <item name="colorOnSecondary">@color/text_on_gold</item>

        <!-- Backgrounds -->
        <item name="android:colorBackground">@color/bg_mint</item>
        <item name="android:windowBackground">@color/bg_mint</item>

        <!-- Status & Nav bar -->
        <item name="android:statusBarColor">@color/bg_light</item>
        <item name="android:navigationBarColor">@color/nav_background</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Text -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Cards -->
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">0dp</item>

        <!-- Buttons -->
        <item name="materialButtonStyle">@style/NammaButton</item>

        <!-- Bottom Navigation -->
        <item name="bottomNavigationStyle">@style/NammaBottomNav</item>

        <!-- Chips -->
        <item name="chipStyle">@style/NammaChip</item>
    </style>

    <!-- No Action Bar Theme for MainActivity -->
    <style name="Theme.NammaMela.NoActionBar" parent="Theme.NammaMela">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Filled Button (Gold) -->
    <style name="NammaButton" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/gold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:letterSpacing">0.05</item>
        <item name="cornerRadius">24dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
    </style>

    <!-- Outlined Button -->
    <style name="NammaButton.Outlined" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/forest_green</item>
        <item name="android:textColor">@color/forest_green</item>
        <item name="cornerRadius">24dp</item>
    </style>

    <!-- Bottom Navigation -->
    <style name="NammaBottomNav" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@color/nav_background</item>
        <item name="itemIconTint">@color/nav_selector</item>
        <item name="itemTextColor">@color/nav_selector</item>
        <item name="elevation">8dp</item>
    </style>

    <!-- Chips (category tags like "Mythological", "Kannada") -->
    <style name="NammaChip" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/chip_bg</item>
        <item name="chipStrokeColor">@color/chip_border</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="android:textColor">@color/chip_text</item>
        <item name="chipCornerRadius">20dp</item>
    </style>

    <!-- Dialog -->
    <style name="ThemeDialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="colorPrimary">@color/forest_green</item>
        <item name="colorAccent">@color/gold</item>
        <item name="buttonBarPositiveButtonStyle">@style/DialogPositiveButton</item>
    </style>

    <style name="DialogPositiveButton" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/forest_green</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>